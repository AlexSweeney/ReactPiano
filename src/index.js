import React from "react";
import ReactDOM from "react-dom";
import Piano from "./PianoContainer.js";

var root = document.querySelector("#root");

ReactDOM.render(
	<div>
		<Piano/>
	</div>,
	root
);